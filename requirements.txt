# Voxylis - Production Dependencies
#
# The version is intentionally not repeated here: config/version.py is the
# single canonical source (run `python -m config.version --print`).

# UI Framework
PyQt5>=5.15.9

# Global Hotkey Listening
pynput>=1.7.6
keyboard>=0.13.5

# Speech-to-Text APIs
groq>=0.4.2
openai>=1.3.9

# Audio Processing
numpy>=1.24.3
scipy>=1.11.4
sounddevice>=0.5.0

# System Integration
pyperclip>=1.8.2

# Secure credential storage (Windows Credential Manager / macOS Keychain /
# Secret Service). Optional: Windows DPAPI via ctypes is used when absent.
keyring>=24.3.0

# Web Server (web/app.py)
flask>=3.0.0
flask-cors>=4.0.0
flask-caching>=2.1.0
flask-limiter>=3.5.0
werkzeug>=3.0.0

# Production WSGI Server
gunicorn>=21.2.0

# Optional error monitoring. Used by web/observability.py (server), the browser
# bundle and utils/observability.py (desktop, opt-in). Every integration
# degrades to a no-op when this is absent, so it is never a hard requirement.
sentry-sdk>=2.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Optional: For building executables
pyinstaller>=6.1.0

# Optional: For development and CI
pytest>=7.4.3
pytest-cov>=4.1.0
black==24.4.2
flake8>=7.0.0
